# ============================================================================
# FASE 04: Utilities e Ferramentas
# ============================================================================
# Utilitários diversos para pipeline
# Instalar APÓS requirements_fase_01.txt, 02 e 03
# ============================================================================

# Denoising (Stage 07)
DeepFilterNet==0.5.6
DeepFilterLib==0.5.6

# Text processing (Stage 06)
textdistance==4.6.3
regex==2025.9.18

# Data manipulation
pandas==2.3.2
pyarrow==21.0.0

# Configuration
python-dotenv==1.1.1
PyYAML==6.0.3
omegaconf==2.3.0

# Progress bars e UI
tqdm==4.67.1
colorama==0.4.6
colorlog==6.9.0

# Logging avançado (opcional)
loguru==0.7.3

# File handling
requests==2.32.5
certifi==2025.8.3
urllib3==2.5.0
charset-normalizer==3.4.3
idna==3.10

# System utilities
psutil==7.1.0
platformdirs==4.4.0

# YAML parsing
ruamel.yaml==0.18.15
ruamel.yaml.clib==0.2.14

# Development tools (opcional)
tabulate==0.9.0

# ============================================================================
# REMOVIDOS (não usados no pipeline):
# - Flask, flask-cors, Werkzeug (sem servidor web)
# - yt-dlp (sem download de vídeos)
# - rich, typer (CLI frameworks não usados)
# - optuna (otimização de hiperparâmetros não usada)
# - asteroid extras (já incluído em fase_03)
# ============================================================================

# ============================================================================
# DEPENDÊNCIAS EXTERNAS (instalar no sistema):
# - SoX: Normalização de áudio (Stage 08)
#   Windows: winget install --id ChrisBagwell.SoX
#   Linux: sudo apt-get install sox
#   MacOS: brew install sox
# ============================================================================
